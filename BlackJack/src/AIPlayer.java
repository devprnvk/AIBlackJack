/**
 * AI Player class extends the Player class with additional operations and tools
 * @author Pranav Kale
 */
public class AIPlayer extends Player {
    public AIPlayer(int initialBalance) {
        super(initialBalance);
    }

}
